#include <stdio.h>
int main(){
    int num;
    scanf("%d",&num);
    if(num>=90 && num<=100){
        printf("A");
        return 0;
    }
    if(num<=89 && num >= 80){
        printf("B");
        return 0;
    }
    if(num>=70 && num < 80){
        printf("C");
        return 0;
    }
    if(num >= 60 && num<70){
    printf("D");
    return 0:
    }
    if(num<60){
        printf("F");
        return 0;
    }
    else:
    printf("Invalid")
    

}